import { ItemProps } from '../types';

interface IProps {
  channel: ItemProps;
}

const ChannelCard = ({ channel }: IProps) => {
  return (
    <div>ChannelCard</div>
  )
}

export default ChannelCard
