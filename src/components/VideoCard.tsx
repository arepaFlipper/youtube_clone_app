import { ItemProps } from '../types';

interface IProps {
  video: ItemProps;
}

const VideoCard = ({ video }: IProps) => {
  return (
    <div>
      VideoCard
    </div>
  )
}

export default VideoCard;
